meta:
  environment: python
  toolchain:
    name: pip
    version: "3.11"
build:
  requirementsPath: requirements.txt
  useCache: true
run:
  command: gunicorn --bind 0.0.0.0:5000 wsgi:app
  persistenceMount: /data
  containerPort: 5000
